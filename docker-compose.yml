version: '3'
services:
  my_notebook:
    build: ./my_notebook
    image: my_notebook
    user: jovyan
    ports:
      - 8888:8888
      - 8501:8501
    environment:
      - DISPLAY=:99
      - USE_HEADLESS_BROWSER=1
    volumes:
      - .:/home/jovyan/work
